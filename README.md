# Projet INFO-F305 (Modélisation et simulation)

Dashboard permettant de visualiser certains théorèmes 

```bash
python -m venv .venv
source .venv/bin/acticate
pip install -r requirements.txt
python src/main.py
```